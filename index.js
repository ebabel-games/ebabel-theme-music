'use strict';

/**
 * `ebabelThemeMusic`
 * Attach audio to the player 3D camera.
 */
const ebabelThemeMusic = require('./src/ebabel-theme-music');

module.exports = {
  ebabelThemeMusic,
};
